# API server port
API_PORT=8080

# Redis hostname (use 'redis' for docker-compose networking)
REDIS_HOST=redis

# Redis service port
REDIS_PORT=6379

# Cache TTL in seconds for product entries
CACHE_TTL_SECONDS=3600

# Primary database connection URL
DATABASE_URL=sqlite:///./products.db
